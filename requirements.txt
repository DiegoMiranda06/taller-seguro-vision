# Deps base para modo demo (laptop, cualquier OS) — sin librerías de Raspberry Pi.
# Para el backend recomendado en el ThinkPad T480 (Intel, sin GPU dedicada),
# instalar también el extra: pip install .[intel]
# Reglas No Negociables #6 (CLAUDE.md): este archivo NUNCA debe incluir
# gpiozero, picamera2, pycoral ni tflite-runtime — esas van solo en
# pyproject.toml bajo el extra [pi].
ultralytics>=8.2
opencv-python>=4.9
numpy>=1.26
pydantic>=2.6
